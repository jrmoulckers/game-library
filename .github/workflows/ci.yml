name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: Format check
        shell: bash
        run: test -z "$(gofmt -l .)"
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...
      - name: Check committed reports for absolute personal paths
        shell: pwsh
        run: |
          $matches = Get-ChildItem reports/baseline -Recurse -File -ErrorAction SilentlyContinue |
            Select-String -Pattern '([A-Za-z]:\\Users\\|/home/[^/]+/|userdata\\[0-9]+|userdata/[0-9]+)'
          if ($matches) {
            $matches | ForEach-Object { Write-Error $_ }
            exit 1
          }

  # Formatting for the six embedded browser ES modules. Separate from `test` so a
  # formatting failure never masks a Go failure, and so the Go job stays runnable
  # without Node. Actions below are SHA-pinned per engineering ENG-SEC-002; the
  # pins in `test` above are addressed on the lint-adoption branch.
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci
      # Ordered deliberately: verify the vendored config still matches its lock
      # BEFORE formatting. Reversed, a drifted config surfaces as hundreds of
      # format diffs and reads as a source problem instead of a config problem.
      - name: Vendored engineering configs match lock
        run: npm run configs:check
      - name: Prettier check
        run: npm run format:check
      # Advisory only. A tag published upstream must never redden an unrelated PR
      # (practices/go.md), and a network failure here is not a repo defect.
      - name: Upstream config drift notice
        continue-on-error: true
        run: bash scripts/vendor-engineering-configs.sh --notify-drift
